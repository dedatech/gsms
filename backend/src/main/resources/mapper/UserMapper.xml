<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsms.gsms.repository.UserMapper">
    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.gsms.gsms.model.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="department_id" property="departmentId"/>
        <result column="status" property="status" 
                typeHandler="com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <!-- 查询所有字段 -->
    <sql id="selectAllFields">
        id, username, password, nickname, email, phone, department_id, status, create_time, update_time, is_deleted
    </sql>

    <!-- 根据ID查询用户 -->
    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM `sys_user`
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM `sys_user`
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="UserResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM `sys_user`
        WHERE is_deleted = 0
    </select>

    <!-- 根据条件查询用户 -->
    <select id="findAll" resultMap="UserResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM `sys_user`
        <where>
            AND is_deleted = 0
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.gsms.gsms.model.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `sys_user`(username, password, nickname, email, phone, department_id, status)
        VALUES(#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{departmentId}, 
               #{status, typeHandler=com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler})
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.gsms.gsms.model.entity.User">
        UPDATE `sys_user`
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="nickname != null and nickname != ''">nickname = #{nickname},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="status != null">status = #{status, typeHandler=com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID逻辑删除用户 -->
    <update id="deleteById" parameterType="long">
        UPDATE `sys_user` SET is_deleted = 1, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <!-- 根据部门ID查询用户列表 -->
    <select id="selectByDepartmentId" parameterType="long" resultMap="UserResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM `sys_user`
        WHERE department_id = #{departmentId} AND is_deleted = 0
    </select>
</mapper>