<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsms.gsms.repository.RoleMapper">

    <resultMap id="RoleResultMap" type="com.gsms.gsms.model.entity.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="description" property="description"/>
        <result column="role_level" property="roleLevel"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="selectAllFields">
        id, name, code, description, role_level, create_time, update_time, create_user_id, update_user_id, is_deleted
    </sql>

    <!-- 根据用户ID查询其拥有的角色编码列表 -->
    <select id="selectRoleCodesByUserId" parameterType="long" resultType="string">
        SELECT r.code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.is_deleted = 0 AND ur.is_deleted = 0
    </select>

    <!-- 根据ID查询角色 -->
    <select id="selectById" parameterType="long" resultMap="RoleResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM sys_role
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <!-- 根据编码查询角色 -->
    <select id="selectByCode" parameterType="string" resultMap="RoleResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM sys_role
        WHERE code = #{code} AND is_deleted = 0
    </select>

    <!-- 根据条件查询角色列表 -->
    <select id="selectByCondition" resultMap="RoleResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM sys_role
        WHERE is_deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="code != null and code != ''">
            AND code LIKE CONCAT('%', #{code}, '%')
        </if>
        <if test="roleLevel != null">
            AND role_level = #{roleLevel}
        </if>
        ORDER BY id
    </select>

    <!-- 查询所有角色 -->
    <select id="selectAll" resultMap="RoleResultMap">
        SELECT <include refid="selectAllFields"/>
        FROM sys_role
        WHERE is_deleted = 0
        ORDER BY role_level, id
    </select>

    <!-- 插入角色 -->
    <insert id="insert" parameterType="com.gsms.gsms.model.entity.Role"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(name, code, description, role_level, create_user_id, update_user_id)
        VALUES(#{name}, #{code}, #{description}, #{roleLevel}, #{createUserId}, #{updateUserId})
    </insert>

    <!-- 更新角色 -->
    <update id="update" parameterType="com.gsms.gsms.model.entity.Role">
        UPDATE sys_role
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="code != null and code != ''">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            <if test="roleLevel != null">role_level = #{roleLevel},</if>
            <if test="updateUserId != null">update_user_id = #{updateUserId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除角色（软删除） -->
    <update id="deleteById" parameterType="long">
        UPDATE sys_role
        SET is_deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 查询角色的权限ID列表 -->
    <select id="selectPermissionIdsByRoleId" parameterType="long" resultType="long">
        SELECT rp.permission_id
        FROM sys_role_permission rp
        WHERE rp.role_id = #{roleId} AND rp.is_deleted = 0
    </select>

    <!-- 删除角色的所有权限 -->
    <update id="deletePermissionsByRoleId" parameterType="long">
        UPDATE sys_role_permission
        SET is_deleted = 1
        WHERE role_id = #{roleId}
    </update>

    <!-- 批量插入角色权限关联 -->
    <insert id="insertRolePermissions">
        INSERT INTO sys_role_permission(role_id, permission_id)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId})
        </foreach>
    </insert>

    <!-- 删除角色权限关联 -->
    <update id="deleteRolePermission">
        UPDATE sys_role_permission
        SET is_deleted = 1
        WHERE role_id = #{roleId} AND permission_id = #{permissionId}
    </update>

    <!-- 查询拥有该角色的用户ID列表 -->
    <select id="selectUserIdsByRoleId" parameterType="long" resultType="long">
        SELECT ur.user_id
        FROM sys_user_role ur
        WHERE ur.role_id = #{roleId} AND ur.is_deleted = 0
    </select>

    <!-- 查询用户的角色ID列表 -->
    <select id="selectRoleIdsByUserId" parameterType="long" resultType="long">
        SELECT ur.role_id
        FROM sys_user_role ur
        WHERE ur.user_id = #{userId} AND ur.is_deleted = 0
    </select>

    <!-- 删除用户的所有角色 -->
    <update id="deleteUserRoles" parameterType="long">
        UPDATE sys_user_role
        SET is_deleted = 1
        WHERE user_id = #{userId}
    </update>

    <!-- 批量插入用户角色关联 -->
    <insert id="insertUserRoles">
        INSERT INTO sys_user_role(user_id, role_id)
        VALUES
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <!-- 删除用户角色关联 -->
    <update id="deleteUserRole">
        UPDATE sys_user_role
        SET is_deleted = 1
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </update>

</mapper>
